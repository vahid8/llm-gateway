# Keep this directory in the repo, but never commit the secret files it holds.
# docker-compose mounts files here as Docker secrets (default path for
# DOUBLEWORD_KEY_FILE). Put real key files here, or point DOUBLEWORD_KEY_FILE
# at ~/.secrets/<key> instead.
*
!.gitignore
